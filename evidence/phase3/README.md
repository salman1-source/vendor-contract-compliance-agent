# Phase 3A evidence

> **These are scripted-model test artifacts, not live OpenAI evidence.**

`generate_phase3_evidence.py` creates the graph export and success, retry, and exhaustion artifacts from actual LangGraph runs. Phase 3B remains pending until the manual GitHub workflow produces separately retained evidence.
