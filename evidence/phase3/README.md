# Phase 3A evidence

> **These are scripted-model test artifacts, not live provider evidence.**

`generate_phase3_evidence.py` creates the graph export and success, retry, and exhaustion artifacts from actual LangGraph runs. Live OpenAI, OpenRouter, or Gemini evidence is created only by a manually selected workflow run and is stored in a provider-specific artifact; it is not committed here.
